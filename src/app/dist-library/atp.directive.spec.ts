import { AtpDirective } from './atp.directive';

describe('AtpDirective', () => {
  it('should create an instance', () => {
    const directive = new AtpDirective();
    expect(directive).toBeTruthy();
  });
});
